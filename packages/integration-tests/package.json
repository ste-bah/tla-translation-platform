{
  "name": "@tla/integration-tests",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:bench": "vitest run --include 'tests/benchmarks/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tla/ingestion": "workspace:*",
    "@tla/registry": "workspace:*",
    "@tla/shared": "workspace:*",
    "@tla/translator": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vitest": "^3.1.1"
  }
}
