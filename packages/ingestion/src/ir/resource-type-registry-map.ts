/**
 * Maps Terraform resource types to registry aws_service keys.
 *
 * CANONICAL SOURCE: @tla/shared/registry-key-resolver.ts
 * This file re-exports from shared for backward compatibility.
 * New consumers should import from @tla/shared directly.
 */

export { resolveRegistryKey, RESOURCE_TYPE_REGISTRY_MAP } from '@tla/shared';
